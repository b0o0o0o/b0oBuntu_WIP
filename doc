---

default_packages:
# APT transport for downloading via the HTTP Secure protocol
  - apt-transport-https
# For analyzing, reverse engineering, and extracting firmware images
  - binwalk
# Everything required for compiling in C and C++.
# gcc / g++ / libc6-dev / make / dpkg-dev)
  - build-essential
#A deb package that contains certificates provided by the Certificate Authorities.
  - ca-certificates
# Counts blank lines, comment lines, and physical lines 
# of source code in many programming languages.
  - cloc
# CMake is a generator of build systems.
  - cmake
# Provides the CMake curses interface. Project configuration through this GUI.
  - cmake-curses-gui
# For getting or sending data including files using URL syntax. 
# Since curl uses libcurl, it supports every protocol libcurl supports.
  - curl
# DOS/MAC to UNIX text file format converter
  - dos2unix
# FFmpeg is a suite of libraries and programs for handling:
# Video / Audio / other multimedia files/streams
# FFmpeg includes : 
# - ff
# - ffplay, a simple media player
# - ffprobe, a command-line tool to display media information
  - ffmpeg
# Pre-patched and adjusted fonts for usage with the Powerline statusline plugin.
  - fonts-powerline
# Forensic data recovery program for Linux
  - foremost
# Small and simple webcam for *nix.
  - fswebcam
# The GNU Debugger (GDB) is a portable debugger
  - gdb
# Package use for internationalization and localization
  - gettext
# This is git !
  - git
# Replacement for Symantec's cryptographic software suite PGP. (use gpg)
  - gnupg
# Identify the different types of hashes used to encrypt data and passwords.
  - hashid
# A command-line hex viewer
  - hexyl
# This is htop, a cross-platform interactive process viewer
  - htop
# Prints information about your hardware
  - hwinfo
# Software suite, used for editing and manipulating digital images.
  - imagemagick
# This is a package of some commandline utilities relating to inotify.
  - inotify-tools
# iproute2 is a collection of userspace utilities for 
# controlling and monitoring various aspects of networking 
# iproute2 collection contains the following command-line utilities: 
# arpd, bridge, ctstat, dcb, devlink, ip, lnstat, nstat, rdma, routef,
# routel, rtacct, rtmon, rtstat, ss, tc, tipc and vdpa.
  - iproute2
# jq is like sed for JSON data
  - jq
# Some Libzzz
  - libbz2-dev
  - libcurl4-openssl-dev
  - libedit-dev
  - libffi-dev
  - libgd-dev
  - libicu-dev
  - libimage-exiftool-perl
  - libjpeg-dev
  - libleptonica-dev
  - liblzma-dev
  - libmysqlclient-dev
  - libncursesw5-dev
  - libonig-dev
  - libpcap-dev
  - libpng-dev
  - libpq-dev
  - libreadline-dev
  - libsqlite3-dev
  - libssl-dev
  - libtesseract-dev
  - libxml2-dev
  - libxml2-utils
  - libxmlsec1-dev
  - libyaml-dev
  - libzip-dev
# Unix utility which serves to find files on filesystems
# It searches through a prebuilt database of files generated by the updatedb command
  - locate
# Linux Standard Base (LSB) details and distribution-specific information
  - lsb-release
# lsof is a command for LiSting Open Files.
  - lsof
# ltrace intercepts and records dynamic library calls which are called by an executed 
# process and the signals received by that process.
  - ltrace
# Commonly used for build automation to build executable code from source code. 
  - make
# Meld is a visual diff and merge tool
# It allows users to compare two files or directories visually, 
# color-coding the different lines.
  - meld
# net-tools is a collection of programs that form the base set of the NET-3 networking 
# distribution for the Linux operating system.
  - net-tools
# ngrep (network grep) is a network packet analyzer 
  - ngrep
# Nmap (Network Mapper) is a network scanner 
  - nmap
# OpenSSH is a powerful collection of tools for remotely controlling networked 
# computers and transferring data between them.
  - openssh-server
# OpenSSL is a software library for applications that provide secure communications 
# computer networks against eavesdropping, and identify the party at the other end. 
# It is widely used by Internet servers, including the majority of HTTPS websites.
  - openssl
# GNU parallel is a command-line utility for Linux and other Unix-like operating  
# systems which allows the user to execute shell scripts or commands in parallel. 
  - parallel
# pkg-config helps you insert the correct compiler options on the command line so 
# an application can use gcc -o test test.c `pkg-config --libs --cflags glib-2.0` 
# for instance, rather than hard-coding values on where to find glib (or other).
  - pkg-config
# Powerline is a statusline plugin for vim, and provides statuslines and prompts for 
# several other applications zsh, bash, fish, tmux, IPython, Awesome, i3 and Qtile...
  - powerline
# PYTHON
  - python3-dev
  - python3-pip
  - python3-venv
# re2c is a free and open-source lexer generator for C/C++, Go and Rust.
  - re2c
# ripgrep is a line-oriented search tool that recursively searches the current  
# directory for a regex pattern.
  - ripgrep
# rlwrap is a 'readline wrapper', a small utility that uses the GNU Readline library
# to allow the editing of keyboard input for any command.
  - rlwrap
# Socat is a multipurpose relay tool for Linux, "The advanced version of netcat". 
  - socat
# software-properties-common is a package that provides an abstraction of the used 
# apt repositories.
  - software-properties-common
# sshpass utility helps administrators more easily manage SSH connections in scripts.
  - sshpass
# Tk-dev is a development package for the Tk toolkit, a cross-platform graphical 
# toolkit implemented in the Tcl scripting language.
  - tk-dev
# tmate Instant terminal sharing via ssh link <3
  - tmate
# tmux is a terminal multiplexer. It lets you switch easily between several programs
# in one terminal, detach them (they keep running in the background) and reattach  
# them to a different terminal.
  - tmux
# tor is Tor is a free and open-source software that enables anonymous communication 
# by routing internet traffic through a network of volunteer-operated servers called
# nodes.
  - tor
# traceroute is a network diagnostic command-line tool used to display the path and 
# transit delays of packets across an Internet Protocol (IP) network.
  - traceroute
# tree displays the directory structure
  - tree
# ufw is a Linux firewall management tool that simplifies the process of configuring
# netfilter rules. It provides a user-friendly command-line interface and uses 
# iptables for configuration.
  - ufw
# To unzip files in Linux
  - unzip
# vbindiff is a tool for visually comparing and highlighting differences between two 
# binary files.
  - vbindiff
# It's VIM
  - vim
# wget is used for retrieving files using HTTP, HTTPS, FTP, and FTPS protocols.
  - wget
# wl-clipboard provides two tools: wl-copy and wl-paste. These utilities allow you 
# to easily copy data between the clipboard and Unix pipes, sockets, files, 
# and other sources.
  - wl-clipboard
# Xclip is a command-line utility that provides an interface to X11 selections, 
# also known as the clipboard. 
  - xclip
# XZ Utils: A data compression software included in common Linux distributions, 
# playing a crucial role in compressing various file formats 
  - xz-utils
# To create a zip archive of a directory, or add a file
  - zip
# zlib1g-dev is a development package for the zlib compression library
  - zlib1g-dev
# Zsh (Z shell) is a Unix shell that serves as an interactive login shell and a 
# command interpreter for shell scripting.
  - zsh

default_packages_gone:
# trash-cli is a command-line utility that moves files and folders to the trash, 
# rather than permanently deleting them like rm.
  - trash-cli # Repo is outdated, pipx install is latest
# regolith-rofication is a minimalistic notification system for Linux desktop 
# environments.
  - regolith-rofication # For dunst notifications replacement

# Beautify and Addons for ZSH
zsh_git_url: 'https://github.com/robbyrussell/oh-my-zsh.git'
zsh_completion_git_url: 'https://github.com/zsh-users/zsh-completions'
zsh_autosuggestion_git_url: 'https://github.com/zsh-users/zsh-autosuggestions'
zsh_syntax_git_url: 'https://github.com/zsh-users/zsh-syntax-highlighting.git'

# Fzf (Finder Fuzzily) is a general-purpose command-line fuzzy finder tool that helps
# you quickly search and open files, commands, processes, hostnames, bookmarks, 
# git commits, and more from the terminal.
fzf_git_url: 'https://github.com/junegunn/fzf.git'

# Configuration TMUX
tmux_conf: |
      set-option -g mouse off
      set-option -g history-limit 5000
      set-option -g status-bg colour231


### INSTALL SOME TOOLS WITH MISE

mise_tools:
# awscli is a simple and powerful tool that enables you to manage and automate your
# AWS services from the command line.
  - awscli
# bat is a cat command clone with modern features such as syntax highlighting, 
# Git integration, automatic paging, and display of non-printable characters.
  - bat
# Direnv is a Linux tool that simplifies the management of environment variables
  - direnv
# eye-pleasing view of disk usage and free space. It is designed to be a replacement 
# for the traditional df command
  - duf
# Glow is a terminal based markdown reader designed from the ground up to bring out 
# the beauty and power of the CLI.
  - glow
# Golang Ecosystem
  - golang
# Helm is an open-source package manager for Kubernetes that automates the deployment
# of software in a simple and consistent way.
  - helm
# JAVA Ecosystem
  - java
# jless is a command-line JSON viewer designed for reading, exploring, and searching 
# through JSON data.
  - jless
# k9s is a terminal-based UI for managing Kubernetes clusters. It provides a real-time
# view of the cluster, simplifying navigation, observation, and management of 
# Kubernetes resources.
  - k9s
# Kubernetes ecosystem
  - kubectl
  - kubectx
# Maven is a build automation tool primarily used for Java projects
  - maven
# Minikube is a local Kubernetes implementation that creates a virtual machine (VM) 
# on your Linux machine and deploys a simple cluster containing one node.
  - minikube
# hyperextensible Vim-based text editor
  - neovim
# Node Ecosystem
  - nodejs
# PDM (Python Dependency Manager) is a modern Python package manager
  - pdm
# Python Ecosystem
  - python
# Rust Ecosystem
  - rust
# Terraform is an infrastructure as code tool developed by HashiCorp, allowing you 
# to safely and predictably provision and manage infrastructure in any cloud or 
# on-premises environment.
  - terraform
# WebSocat is designed to provide a Web-based interface for Socat, allowing users to 
# interact with its features and functions through a web browser.
  - websocat


#### HERE SOME CONFIGURATION 

omz_plugins_to_enable:
  - aws
  - colored-man-pages
  - direnv
  - docker
  - extract
  - fzf
  - helm
  - kubectl
  - kubectx
  - minikube
  - mise # former rtx https://github.com/jdx/mise
  - npm
  - terraform
  - tmux
  - zsh-autosuggestions
  - zsh-completions
  - zsh-syntax-highlighting

zsh_plugins_to_disable:
  - rtx # cause fzf replacement
  - ssh-agent

zsh_config_lines:
  - DISABLE_MAGIC_FUNCTIONS=true

vimrc_content: |
  set mouse=""
  set number
  set paste
  syntax on

### INSTALL SOME TOOLS WITH PIPX

pipx_tools:
# bypass-url-parser is a tool designed to test URL parsing vulnerabilities in 
# web applications. 
  - bypass-url-parser
# Dirsearch is a Python-based command-line tool designed to brute-force 
# web server folders and files. 
  - dirsearch
# sqlmap is an open-source penetration testing tool for automated detection 
# and exploitation of SQL injection vulnerabilities.
  - sqlmap
# trash-cli is a command-line utility that moves files and folders to the trash, 
# rather than permanently deleting them like rm.
  - trash-cli
# Wafw00f is a Linux-based security tool used for web application firewall (WAF) 
# detection, fingerprinting, and bypassing. 
  - wafw00f

### INSTALL SOME TOOLS WITH CARGO

cargo_tools:
# Bottom is a terminal-based graphical dashboard for system monitoring, providing 
# real-time information about CPU, RAM, disk, network usage, and more. 
  - bottom
# Dust is a Rust-based command-line tool designed to provide an instant overview 
# of disk space usage within directories. 
  - du-dust
# Exa is designed to replace the traditional ls command on Linux
  - exa
# git-delta is a diff viewer
  - git-delta
# Htmlq is a command-line tool for parsing and extracting data from HTML pages, 
# similar to jq but for HTML
  - htmlq
# viu is designed to view images from the terminal.
  - viu
# xsv is a command-line based CSV processor that allows for various operations 
# such as searching, slicing, splitting, joining, sorting, and counting
  - xsv
# Asciiinema is a command-line tool for recording terminal sessions on Linux.
  - --git https://github.com/asciinema/asciinema

### DOCKER INSTALL

docker_gpg_dest: '/usr/share/keyrings/docker-archive-keyring.gpg'
docker_key_url: 'https://download.docker.com/linux/ubuntu/gpg'
docker_deb_url: 'deb [arch=amd64 signed-by={{ docker_gpg_dest }}] https://download.docker.com/linux/ubuntu jammy stable'
docker_components: 
  - docker-ce
  - docker-ce-cli
  - containerd.io
  - docker-buildx-plugin
  - docker-compose-plugin

